{
  "name": "fullstack-app",
  "version": "1.0.0",
  "description": "A fullstack example of a pnpm workspace.",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --parallel --stream -r run dev",
    "build": "pnpm --stream -r run build",
    "clean": "rm -rf && pnpm -r run clean"
  },
  "keywords": [],
  "author": "https://github.com/CodeRigged",
  "license": "MIT",
  "devDependencies": {
    "buffer": "^6.0.3"
  }
}
